import { types } from './field';
import { Permission } from './permissions';
import { Relation } from './relation';
declare type CollectionsOverview = {
    [name: string]: {
        collection: string;
        primary: string;
        singleton: boolean;
        sortField: string | null;
        note: string | null;
        accountability: 'all' | 'activity' | null;
        fields: {
            [name: string]: {
                field: string;
                defaultValue: any;
                nullable: boolean;
                type: typeof types[number] | 'unknown' | 'alias';
                precision: number | null;
                scale: number | null;
                special: string[];
                note: string | null;
                alias: boolean;
            };
        };
    };
};
export declare type SchemaOverview = {
    collections: CollectionsOverview;
    relations: Relation[];
    permissions: Permission[];
};
export {};
